# Supabase (public, client-safe keys)
# Get these from Supabase Dashboard > Settings > API
# The anon key is safe to expose in the browser; never use the service_role key here.
VITE_SUPABASE_URL=
VITE_SUPABASE_ANON_KEY=
